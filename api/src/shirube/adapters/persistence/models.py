"""ORM models for Shirube's local app state.

No tables exist yet — they arrive with the features that need them (connection
profiles, node layout, manual relationships, chat history). Importing this module
registers those models on ``Base.metadata``.
"""
