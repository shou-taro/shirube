# domain

Pure business types and rules — value objects (schema, table, column, relationship,
connection profile), invariants, and domain services. No framework or I/O
dependencies: nothing here imports FastAPI, SQLAlchemy, or any HTTP/DB library.
